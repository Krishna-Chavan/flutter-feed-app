android {
    compileSdkVersion 31

    defaultConfig {
        applicationId "com.example.flutterfeedapp"
        minSdkVersion 21
        targetSdkVersion 31
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }
}
